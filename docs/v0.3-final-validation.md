# v0.3 Final Validation

## Scope

Final validation for Sovereign AI SOC v0.3 after RBAC, audit logging, session hardening, frontend authorization alignment, security headers, secrets/config hardening, PostgreSQL 18.4 migration, Wazuh ingest recovery and UX refinements.

## Validated areas

- Backend API startup
- Frontend production build
- PostgreSQL 18.4 connectivity
- Wazuh Indexer connectivity
- Wazuh ingest worker heartbeat
- Incident ingestion
- Platform health
- Security audit events
- RBAC and role-aware frontend behavior
- Nginx security headers
- Case Queue UX
- Case Detail UX
- Sidebar navigation

## Runtime state

- PostgreSQL image: `postgres:18.4`
- PostgreSQL container: `postgres-soc`
- PostgreSQL volume: `postgres_soc_data_pg18`
- PostgreSQL bind address: `127.0.0.1:5432`
- API service: `ai-soc-api`
- Frontend service: `ai-soc-frontend`
- Worker service: `ai-soc-worker`

## Wazuh ingest hardening

The Wazuh ingest worker was hardened to reduce false health warnings and reduce the risk of being stuck in duplicate-only batches.

Runtime tuning:

- `WAZUH_BATCH_SIZE=150`
- `WAZUH_WATERMARK_OVERLAP_SECONDS=15`
- `WAZUH_INITIAL_LOOKBACK_MINUTES=60`
- `WORKER_STALE_AFTER_SECONDS=300`

## Validation result

Status: PASSED

The system is ready to proceed toward v0.3 closure and release preparation.
